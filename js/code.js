// if (sample_text === "") {
//   return;
// }

// for (var key in counter) {
//   if (key === sample_text[0].toLowerCase()) {
//     counter[key]++;
//   }
// }

// countLetters(counter, sample_text.substr(1));
// return counter;
